const GalleryFilters = ({ filters, metadata, onChange, onReset }) => <div className="grid gap-3 rounded-lg border border-gray-200 bg-white p-4 sm:grid-cols-2 lg:grid-cols-5"><input aria-label="Search gallery" value={filters.search} onChange={(event) => onChange("search", event.target.value)} placeholder="Search people or categories" className="rounded border border-gray-300 px-3 py-2 lg:col-span-1" />{[["year", "All years", metadata?.years], ["cohort", "All cohorts", metadata?.cohorts], ["category", "All categories", metadata?.categories], ["role", "All people", ["student", "trainer"]]].map(([key, label, options]) => <select key={key} aria-label={label} value={filters[key]} onChange={(event) => onChange(key, event.target.value)} className="rounded border border-gray-300 bg-white px-3 py-2"><option value="">{label}</option>{options?.map((option) => <option key={option} value={option}>{option}</option>)}</select>)}<button type="button" onClick={onReset} className="rounded border border-primary px-3 py-2 font-medium text-primary hover:bg-primary hover:text-white">Clear filters</button></div>;
export default GalleryFilters;
